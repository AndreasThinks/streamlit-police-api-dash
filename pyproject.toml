[project]
name = "police-api-streamlit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.31",
    "pandas>=2.1",
    "numpy>=1.26",
    "plotly>=5.18",
    "scikit-learn>=1.3",
    "umap-learn>=0.5.5",
    "hdbscan>=0.8.33",
    "bertopic>=0.16",
    "kaggle>=1.6",
]

# Note: We install bertopic with --no-deps to avoid pulling in sentence-transformers and torch
# The app uses custom TF-IDF embeddings instead of neural embeddings
